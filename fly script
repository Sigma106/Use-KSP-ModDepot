local main = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local TitleBar = Instance.new("Frame")
local TitleLabel = Instance.new("TextLabel")
local up = Instance.new("TextButton")
local down = Instance.new("TextButton")
local onof = Instance.new("TextButton")
local plus = Instance.new("TextButton")
local speed = Instance.new("TextLabel")
local mine = Instance.new("TextButton")
local closebutton = Instance.new("TextButton")
local mini = Instance.new("TextButton")
local mini2 = Instance.new("TextButton")

-- Parent & properties
main.Name = "main"
main.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
main.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
main.ResetOnSpawn = false

Frame.Parent = main
Frame.BackgroundColor3 = Color3.fromRGB(34, 34, 34) -- dark grey
Frame.BorderColor3 = Color3.fromRGB(187, 187, 187) -- light grey
Frame.Position = UDim2.new(0.1, 0, 0.38, 0)
Frame.Size = UDim2.new(0, 190, 0, 57)
Frame.Active = true
Frame.Draggable = false -- we'll drag TitleBar instead

-- TitleBar for dragging
TitleBar.Name = "TitleBar"
TitleBar.Parent = Frame
TitleBar.BackgroundColor3 = Color3.fromRGB(17, 17, 17) -- darker black
TitleBar.BorderColor3 = Color3.fromRGB(187, 187, 187)
TitleBar.Size = UDim2.new(1, 0, 0, 28)
TitleBar.Active = true
TitleBar.Draggable = true

TitleLabel.Name = "TitleLabel"
TitleLabel.Parent = TitleBar
TitleLabel.BackgroundTransparency = 1
TitleLabel.Position = UDim2.new(0.25, 0, 0, 0)
TitleLabel.Size = UDim2.new(0.5, 0, 1, 0)
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.Text = "FLY GUI V3"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextScaled = true
TitleLabel.TextWrapped = true

-- Buttons and labels (all with black background, white text)
local function styleButton(btn)
	btn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	btn.BorderColor3 = Color3.fromRGB(187, 187, 187)
	btn.Font = Enum.Font.SourceSans
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.TextSize = 14
	btn.AutoButtonColor = true
end

up.Name = "up"
up.Parent = Frame
up.Position = UDim2.new(0, 0, 0.491, 0)
up.Size = UDim2.new(0, 44, 0, 28)
up.Text = "UP"
styleButton(up)

down.Name = "down"
down.Parent = Frame
down.Position = UDim2.new(0, 46, 0.491, 0)
down.Size = UDim2.new(0, 44, 0, 28)
down.Text = "DOWN"
styleButton(down)

onof.Name = "onof"
onof.Parent = Frame
onof.Position = UDim2.new(0, 138, 0.491, 0)
onof.Size = UDim2.new(0, 56, 0, 28)
onof.Text = "Fly"
styleButton(onof)

plus.Name = "plus"
plus.Parent = Frame
plus.Position = UDim2.new(0, 46, 0, 0)
plus.Size = UDim2.new(0, 45, 0, 28)
plus.Text = "+"
styleButton(plus)

speed.Name = "speed"
speed.Parent = Frame
speed.BackgroundColor3 = Color3.fromRGB(34,34,34)
speed.BorderColor3 = Color3.fromRGB(187,187,187)
speed.Position = UDim2.new(0, 92, 0.491, 0)
speed.Size = UDim2.new(0, 44, 0, 28)
speed.Font = Enum.Font.SourceSans
speed.Text = "1"
speed.TextColor3 = Color3.fromRGB(255, 255, 255)
speed.TextScaled = true
speed.TextWrapped = true

mine.Name = "mine"
mine.Parent = Frame
mine.Position = UDim2.new(0, 0, 0, 0)
mine.Size = UDim2.new(0, 44, 0, 28)
mine.Text = "-"
styleButton(mine)

closebutton.Name = "Close"
closebutton.Parent = TitleBar
closebutton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
closebutton.BorderColor3 = Color3.fromRGB(187, 187, 187)
closebutton.Position = UDim2.new(1, -44, 0, 0)
closebutton.Size = UDim2.new(0, 44, 0, 28)
closebutton.Font = Enum.Font.SourceSansBold
closebutton.Text = "X"
closebutton.TextColor3 = Color3.fromRGB(255, 255, 255)
closebutton.TextSize = 24
closebutton.AutoButtonColor = true

mini.Name = "minimize"
mini.Parent = TitleBar
mini.BackgroundColor3 = Color3.fromRGB(85, 85, 85)
mini.BorderColor3 = Color3.fromRGB(187, 187, 187)
mini.Position = UDim2.new(1, -88, 0, 0)
mini.Size = UDim2.new(0, 44, 0, 28)
mini.Font = Enum.Font.SourceSansBold
mini.Text = "-"
mini.TextColor3 = Color3.fromRGB(255, 255, 255)
mini.TextSize = 24
mini.AutoButtonColor = true

mini2.Name = "minimize2"
mini2.Parent = TitleBar
mini2.BackgroundColor3 = Color3.fromRGB(85, 85, 85)
mini2.BorderColor3 = Color3.fromRGB(187, 187, 187)
mini2.Position = UDim2.new(1, -88, 0, 0)
mini2.Size = UDim2.new(0, 44, 0, 28)
mini2.Font = Enum.Font.SourceSansBold
mini2.Text = "+"
mini2.TextColor3 = Color3.fromRGB(255, 255, 255)
mini2.TextSize = 24
mini2.AutoButtonColor = true
mini2.Visible = false

-- Fly variables
local speeds = 1
local nowe = false
local tpwalking = false

local player = game:GetService("Players").LocalPlayer
local chr = player.Character or player.CharacterAdded:Wait()
local hum = chr:FindFirstChildWhichIsA("Humanoid")

-- Notification (optional)
game:GetService("StarterGui"):SetCore("SendNotification", {
	Title = "FLY GUI V3";
	Text = "BY XNEO";
	Icon = "rbxthumb://type=Asset&id=5107182114&w=150&h=150"
})

-- Function definitions here (reuse from previous full code you have)
-- For brevity, omitted here; you can copy the toggleFly and button event handlers from your previous script.

-- Connect button events
up.MouseButton1Click:Connect(function()
	if nowe then
		player.Character:TranslateBy(Vector3.new(0, speeds, 0))
	end
end)

down.MouseButton1Click:Connect(function()
	if nowe then
		player.Character:TranslateBy(Vector3.new(0, -speeds, 0))
	end
end)

onof.MouseButton1Click:Connect(function()
	-- toggle fly (define toggleFly function as before)
	toggleFly()
end)

plus.MouseButton1Click:Connect(function()
	speeds = speeds + 1
	speed.Text = tostring(speeds)
end)

mine.MouseButton1Click:Connect(function()
	if speeds > 1 then
		speeds = speeds - 1
		speed.Text = tostring(speeds)
	end
end)

closebutton.MouseButton1Click:Connect(function()
	main:Destroy()
end)

mini.MouseButton1Click:Connect(function()
	Frame.Size = UDim2.new(0, 70, 0, 28)
	up.Visible = false
	down.Visible = false
	onof.Visible = false
	plus.Visible = false
	speed.Visible = false
	mine.Visible = false
	mini.Visible = false
	mini2.Visible = true
	TitleLabel.Visible = false
end)

mini2.MouseButton1Click:Connect(function()
	Frame.Size = UDim2.new(0, 190, 0, 57)
	up.Visible = true
	down.Visible = true
	onof.Visible = true
	plus.Visible = true
	speed.Visible = true
	mine.Visible = true
	mini.Visible = true
	mini2.Visible = false
	TitleLabel.Visible = true
end)

-- Make sure toggleFly function and other fly logic is included from your previous full code.

