-- SERVICES
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local player = Players.LocalPlayer

-- SETTINGS
Players.RespawnTime = 0

-- VARIABLES
local enabled = false
local savedCFrame = nil
local deathConnection = nil

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "FixedTeleportOnDeathGui"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local main = Instance.new("Frame")
main.Size = UDim2.new(0, 340, 0, 310)
main.Position = UDim2.new(0.5, -170, 0.5, -155)
main.BackgroundColor3 = Color3.fromRGB(30,30,30)
main.BorderSizePixel = 0
main.Parent = gui

-- TITLE BAR
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1,0,0,35)
titleBar.BackgroundColor3 = Color3.fromRGB(45,45,45)
titleBar.Parent = main

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,-40,1,0)
title.Position = UDim2.new(0,10,0,0)
title.BackgroundTransparency = 1
title.Text = "FIXED TELEPORT TOOL"
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.GothamBold
title.TextSize = 15
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = titleBar

-- CLOSE
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0,35,1,0)
closeBtn.Position = UDim2.new(1,-35,0,0)
closeBtn.Text = "X"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 18
closeBtn.TextColor3 = Color3.new(1,1,1)
closeBtn.BackgroundColor3 = Color3.fromRGB(170,50,50)
closeBtn.Parent = titleBar

closeBtn.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

-- TEXT BOX
local coordBox = Instance.new("TextBox")
coordBox.Size = UDim2.new(1, -20, 0, 40)
coordBox.Position = UDim2.new(0, 10, 0, 50)
coordBox.PlaceholderText = "X, Y, Z"
coordBox.ClearTextOnFocus = false
coordBox.Font = Enum.Font.Gotham
coordBox.TextSize = 14
coordBox.TextColor3 = Color3.new(1,1,1)
coordBox.BackgroundColor3 = Color3.fromRGB(40,40,40)
coordBox.Parent = main

-- COPY BUTTON
local copyBtn = Instance.new("TextButton")
copyBtn.Size = UDim2.new(1, -20, 0, 35)
copyBtn.Position = UDim2.new(0, 10, 0, 100)
copyBtn.Text = "COPY CORRECT POSITION"
copyBtn.Font = Enum.Font.GothamBold
copyBtn.TextSize = 14
copyBtn.TextColor3 = Color3.new(1,1,1)
copyBtn.BackgroundColor3 = Color3.fromRGB(70,120,170)
copyBtn.Parent = main

-- TELEPORT BUTTON
local tpBtn = Instance.new("TextButton")
tpBtn.Size = UDim2.new(1, -20, 0, 35)
tpBtn.Position = UDim2.new(0, 10, 0, 145)
tpBtn.Text = "TELEPORT HERE"
tpBtn.Font = Enum.Font.GothamBold
tpBtn.TextSize = 14
tpBtn.TextColor3 = Color3.new(1,1,1)
tpBtn.BackgroundColor3 = Color3.fromRGB(170,120,70)
tpBtn.Parent = main

-- START
local startBtn = Instance.new("TextButton")
startBtn.Size = UDim2.new(0.5, -15, 0, 40)
startBtn.Position = UDim2.new(0, 10, 0, 195)
startBtn.Text = "START"
startBtn.Font = Enum.Font.GothamBold
startBtn.TextSize = 16
startBtn.TextColor3 = Color3.new(1,1,1)
startBtn.BackgroundColor3 = Color3.fromRGB(50,170,50)
startBtn.Parent = main

-- STOP
local stopBtn = Instance.new("TextButton")
stopBtn.Size = UDim2.new(0.5, -15, 0, 40)
stopBtn.Position = UDim2.new(0.5, 5, 0, 195)
stopBtn.Text = "STOP"
stopBtn.Font = Enum.Font.GothamBold
stopBtn.TextSize = 16
stopBtn.TextColor3 = Color3.new(1,1,1)
stopBtn.BackgroundColor3 = Color3.fromRGB(170,50,50)
stopBtn.Parent = main

-- DRAGGING
local dragging, dragStart, startPos

titleBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPos = main.Position
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
		local delta = input.Position - dragStart
		main.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = false
	end
end)

-- COPY POSITION → AUTO PASTE
copyBtn.MouseButton1Click:Connect(function()
	local char = player.Character
	if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart")
	if not root then return end

	local pos = root.Position
	coordBox.Text = string.format("%d, %d, %d",
		math.floor(pos.X),
		math.floor(pos.Y),
		math.floor(pos.Z)
	)
	savedCFrame = CFrame.new(pos)
end)

-- TELEPORT NOW
tpBtn.MouseButton1Click:Connect(function()
	if not savedCFrame then return end
	local char = player.Character
	if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart")
	if not root then return end

	root.CFrame = savedCFrame
end)

-- START
startBtn.MouseButton1Click:Connect(function()
	if not savedCFrame then
		startBtn.Text = "COPY POSITION"
		task.wait(1)
		startBtn.Text = "START"
		return
	end
	enabled = true
	startBtn.Text = "RUNNING"
end)

-- STOP
stopBtn.MouseButton1Click:Connect(function()
	enabled = false
	startBtn.Text = "START"
end)

-- CHARACTER HANDLING
local function onCharacter(char)
	local humanoid = char:WaitForChild("Humanoid")
	local root = char:WaitForChild("HumanoidRootPart")

	if enabled and savedCFrame then
		task.wait()
		root.CFrame = savedCFrame
	end

	if deathConnection then
		deathConnection:Disconnect()
	end

	deathConnection = humanoid.Died:Connect(function()
		if enabled then
			task.wait()
			player:LoadCharacter()
		end
	end)
end

player.CharacterAdded:Connect(onCharacter)
if player.Character then
	onCharacter(player.Character)
end
