local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "squit game script ez win",
    LoadingTitle = "loading",
    LoadingSubtitle = "by green_coolkid100",
    ConfigurationSaving = {
       Enabled = true,
       FolderName = nil, -- Create a custom folder for your hub/game
       FileName = "Big Hub"
    },
    Discord = {
       Enabled = false,
       Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD
       RememberJoins = true -- Set this to false to make them join the discord every time they load it up
    },
    KeySystem = false, -- Set this to true to use our key system
    KeySettings = {
       Title = "Untitled",
       Subtitle = "Key System",
       Note = "No method of obtaining the key is provided",
       FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
       SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
       GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
       Key = {"Hello"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
    }
 })
 
local PlayerTab = Window:CreateTab("Player", 7992557358) -- Title, Image

local hide_and_seek_game = Window:CreateTab("hide and seek game") -- Title, Image

local jump_rope_game = Window:CreateTab("jump rope game") -- Title, Image

local teleportsTab = Window:CreateTab("teleports")

local Buttonteleport = PlayerTab:CreateButton({
    Name = "teleport to player gui", -- Button label
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Sigma106/Use-KSP-ModDepot/refs/heads/main/zzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"))()
    end,
})

local Button0 = PlayerTab:CreateButton({
    Name = "tp tool", -- Button label
    Callback = function()
        local Tele = Instance.new("Tool", game.Players.LocalPlayer.Backpack)
Tele.RequiresHandle = false
Tele.RobloxLocked = true
Tele.Name = "TPTool"
Tele.ToolTip = "Teleport Tool"
Tele.Equipped:connect(function(Mouse)
	Mouse.Button1Down:connect(function()
		if Mouse.Target then
			game.Workspace:FindFirstChild(game.Players.LocalPlayer.Name).HumanoidRootPart.CFrame = (CFrame.new(Mouse.Hit.x, Mouse.Hit.y + 5, Mouse.Hit.z))
		end
	end)
end)
    end,
})

local Buttonnoclip = PlayerTab:CreateButton({
    Name = "noclip", -- Button label
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/Sigma106/Use-KSP-ModDepot/refs/heads/main/efeefefewewfewfefffffffffffffffffff'))()
    end,
})

local Button = PlayerTab:CreateButton({
    Name = "fly FlyGuiV3", -- Button label
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
    end,
})

local Button = jump_rope_game:CreateButton({
    Name = "Teleport to the Finish", -- Button label
    Callback = function()
        local player = game.Players.LocalPlayer -- Get the local player
        if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            -- Teleport the player to the desired position
            player.Character.HumanoidRootPart.CFrame = CFrame.new(-242, 106, -187)
        end
    end,
})

local Button01 = hide_and_seek_game:CreateButton({
    Name = "esp", -- Button label
    Callback = function()
       loadstring(game:HttpGet('https://raw.githubusercontent.com/Sigma106/Use-KSP-ModDepot/refs/heads/main/esp%20gui%20on%20off'))()
    end
})

local Button6 = hide_and_seek_game:CreateButton({
    Name = "Safe Zone IF BLUE! And if you red you can kill revives ;)", -- Button label
    Callback = function()
        local player = game.Players.LocalPlayer -- Get the local player
        if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = CFrame.new(1,12,1)
        end
    end,
})

local Button1 = hide_and_seek_game:CreateButton({
    Name = "Teleport to spawn", -- Button label
    Callback = function()
        local player = game.Players.LocalPlayer -- Get the local player
        if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = CFrame.new(2,4,456)
        end
    end,
})

local Buttontopig = teleportsTab:CreateButton({
    Name = "Teleport to pig", -- Button label
    Callback = function()
        local player = game.Players.LocalPlayer -- Get the local player
        if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = CFrame.new(-1,38,-1)
        end
    end,
})

local Butto1414n6 = teleportsTab:CreateButton({
    Name = "Teleport to beds room", -- Button label
    Callback = function()
        local player = game.Players.LocalPlayer -- Get the local player
        if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = CFrame.new(1,12,1)
        end
    end,
})
